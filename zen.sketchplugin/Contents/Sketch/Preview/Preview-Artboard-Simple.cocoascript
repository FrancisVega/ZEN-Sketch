var onRun = function(context) {
  @import "common-preview.js";
  const doc = context.document;
  const exportTemplate = exportArboardsWithTemplate ( context.selection, "simple" );
  if (exportTemplate) {
    [doc showMessage: "🖥 Simple Previz"];
  }
}
