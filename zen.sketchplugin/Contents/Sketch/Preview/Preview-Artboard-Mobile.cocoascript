var onRun = function(context) {
  @import "common-preview.js";
  const doc = context.document;
  const exportTemplate = exportArboardsWithTemplate ( context.selection, "mobile" );
  if (exportTemplate) {
    [doc showMessage: "📱 Mobile Previz"];
  }
}

