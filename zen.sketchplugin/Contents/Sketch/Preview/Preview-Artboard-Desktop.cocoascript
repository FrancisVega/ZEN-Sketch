var onRun = function(context) {
  @import "common-preview.js";
  const doc = context.document;
  const exportTemplate = exportArboardsWithTemplate ( context.selection, "desktop" );
  if (exportTemplate) {
    [doc showMessage: "🖥 Desktop Previz"];
  }
}
